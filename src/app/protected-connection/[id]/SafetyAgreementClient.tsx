"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export default function SafetyAgreementClient({connectionId,alreadyAgreed,status}:{connectionId:string;alreadyAgreed:boolean;status:string}){const router=useRouter();const [checked,setChecked]=useState(false),[saving,setSaving]=useState(false),[error,setError]=useState("");if(alreadyAgreed)return <div style={{padding:".75rem",borderRadius:"10px",background:"#eef5ff",color:"#0b3a70",fontWeight:700}}>Your safety agreement is recorded ✓<small style={{display:"block",fontWeight:400,marginTop:".25rem"}}>{status==="active"?"Both adults have agreed. Contact details are still protected.":"Waiting for the other adult to complete the same safety agreement."}</small></div>;async function agree(){setSaving(true);setError("");const r=await fetch(`/api/protected-connections/${connectionId}/safety-agreement`,{method:"POST"});const j=await r.json();setSaving(false);if(!r.ok){setError(j.error||"Unable to record your agreement.");return;}router.refresh();}return <div><label style={{display:"flex",gap:".65rem",alignItems:"flex-start",margin:".75rem 0"}}><input type="checkbox" checked={checked} onChange={e=>setChecked(e.target.checked)} style={{width:"auto",marginTop:".2rem"}}/><span>I have read these safety requirements and agree to follow them throughout this protected connection.</span></label><button className="button primary" type="button" disabled={!checked||saving} onClick={agree}>{saving?"Recording...":"Agree to Safety Requirements"}</button>{error&&<p style={{color:"#b42318",fontWeight:700}}>{error}</p>}</div>}
